use ant -p to show the available tasks

invoke by creating build.properties with your credentials or pass explicitly like:
 ant -Dservice=ibmdev -Ddriver=ibmdev -Daccount=email  -Dkey=password

see http://code.google.com/p/jclouds/wiki/ComputeGuide#Ant for more
